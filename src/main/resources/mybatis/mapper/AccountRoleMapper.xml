<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="co.kr.mmsoft.mmmemberservice.mybatis.mapper.AccountRoleMapper">
<!--    <select id="selectBlog" resultType="Blog">-->
<!--        select * from Blog where id = #{id}-->
<!--    </select>-->
    <insert id = "insert" parameterType="co.kr.mmsoft.mmmemberservice.mybatis.domain.AccountRole">
        insert into account_role(account_id, role_id)
        values(#{account.accountId},#{role.roleId})
    </insert>

</mapper>
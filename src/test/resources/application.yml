# src/test/resources/application.yml
spring:
  application:
    name: mm-member-service
  config:
    #import: "optional:configserver:http://localhost:1881"  # 테스트 시 Config Server 없어도 무시
    import: "optional:none"
  cloud:
    config:
      enabled: false
      fail-fast: false
